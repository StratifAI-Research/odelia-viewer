# Netlify Config
#
# TOML Reference:
# https://www.netlify.com/docs/netlify-toml-reference/
#
# We use Netlify for deploy previews and for publishing docs (gh-pages branch).
# https://viewer.ohif.org is created using a different process that is
# managed by CircleCI and deployed to our Google Hosting
#

[build]
  base = ""
  build = "bun run build:viewer:ci"
  publish = "dist"


# NODE_VERSION in root `.nvmrc` takes priority
[build.environment]
  # If 'production', devDependencies are not installed
  NODE_ENV = "development"
  NODE_VERSION = "20.18.1"
  BUN_VERSION = "1.2.23"
  BUN_FLAGS = "--frozen-lockfile"
  RUBY_VERSION = "2.6.2"

[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/*"

  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"

    # Multi-key header rules are expressed with multi-line strings.
    cache-control = '''
    max-age=0,
    no-cache,
    no-store,
    must-revalidate'''
